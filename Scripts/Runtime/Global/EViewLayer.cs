namespace Engine.Scripts.Runtime.Global
{
    public enum EViewLayer
    {
        Main = 100,
        Normal = 200,
        Float = 250,
        Alert = 300,
        Transition = 350,
        Mask = 400,
        GM = 500,
        System = 600,
        Tips = 700,
    }
}